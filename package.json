{
  "name": "interview-card-cms",
  "version": "1.0.0",
  "description": "A markdown-based interview experience card CMS with GitHub Pages deployment",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build.js && vite build",
    "preview": "vite preview",
    "deploy": "npm run build",
    "create": "node scripts/create.js"
  },
  "dependencies": {
    "marked": "^11.1.0",
    "terser": "^5.46.1"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "vite": "^5.1.0"
  }
}
